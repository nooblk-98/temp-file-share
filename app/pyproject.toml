[project]
name = "temp-file-share"
version = "1.2.0"
description = "Upload files and folders from your terminal and get a download link."
requires-python = ">=3.9"
dependencies = []

[tool.ty]
python-version = "3.9"

[tool.ruff]
target-version = "py39"
line-length = 100
